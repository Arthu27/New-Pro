# Панель на твоём домене — hakumods.xyz

Как сделать, чтобы **панель Aether открывалась прямо на главном домене**
`https://hakumods.xyz` (а также `www.hakumods.xyz` и `panel.hakumods.xyz`) —
бесплатно, без VPS, без публичного IP и без проброса портов.

Схема: **Cloudflare Tunnel** (именованный, постоянный — адрес не меняется
никогда, туннель сам стартует вместе с Windows). Запрос идёт из браузера в
ближайший дата-центр Cloudflare по защищённому HTTPS (замочек — из коробки),
а оттуда по шифрованному туннелю прямо к панели на твоём ПК (`localhost:5001`).

> ⚠️ Старую страницу-заглушку домена убираем (ты сказал — не нужна). Файлы на
> хостинге nska остаются нетронутыми — если позже захочешь вернуть промо-сайт,
> это делается одной DNS-записью, просто скажи.

---

## Шаг 1. Аккаунт Cloudflare и перенос DNS (≈10 минут потом ожидание)

1. Зайди на https://dash.cloudflare.com и зарегистрируйся (бесплатно).
2. **Add site** → введи `hakumods.xyz` → план **Free** → дальше.
3. Сканирование записей: **A-запись сайта (65.108.15.136) можно удалить** —
   сайт-пустышка больше не нужен, наш скрипт сам создаст нужные записи.
4. Cloudflare покажет **2 своих NS-сервера** (вида `xxxx.ns.cloudflare.com`) —
   сохрани их (сфоткай).
5. В биллинге hostia (где покупал домен): домен `hakumods.xyz` → DNS-серверы →
   **замени** `ns37.nska.net` / `ns38.nska.net` на те два от Cloudflare → сохрани.
6. Жди `Active` в Cloudflare — обычно 15 минут … пара часов (придёт письмо).

## Шаг 2. Скрипт на компьютере (≈5 минут)

1. Обнови папку бота свежим ZIP (Code → Download ZIP → распаковать с заменой).
2. Убедись, что бот запущен (`start.bat`) — скрипт проверит панель сам
   и остановится с подсказкой, если она не отвечает.
3. **ПКМ по `scripts\setup_panel_tunnel.bat` → «Запуск от имени администратора»**.
4. Скрипт делает всё сам: проверяет панель → качает cloudflared → открывает
   браузер (нажми **Authorize** на hakumods.xyz) → создаёт туннель → привязывает
   все три адреса → ставит службу Windows с автозапуском → **сам дописывает**
   `WEB_BEHIND_PROXY=1` в твой `.env` → запускает службу.
5. Увидишь `DONE! Panel now lives on your domain` (скрипт пишет по-английски —
   так надёжно: русский текст в .bat Windows иногда ломает кодировкой).

## Важный бонус: туннель запускается ВМЕСТЕ с ботом

После обновления бот сам поднимает туннель при каждом запуске `start.bat` —
двойной клик, и работает ВСЁ сразу: бот + панель + домен. В консоли видишь
строки `[ТУННЕЛЬ] ...`. Если туннель уже стоит службой Windows (вариант
автозапуска из скрипта) — бот это видит и не запускает второй. Отключить
совсем: `TUNNEL_AUTOSTART=0` в `.env`.

Старое поведение со случайной ссылкой `*.trycloudflare.com` (менялась каждый
запуск) **выключено**: в канал `aether-panel` бот теперь отправляет постоянную
ссылку `https://hakumods.xyz`. Вернуть случайную — `QUICK_TUNNEL=1` в `.env`.

## Режим «только start.bat» (VDS)

Настраивать на новой машине ничего не нужно: бот при старте сам докачивает
`cloudflared` под свою ОС (раз), сам чинит путь к ключу туннеля, сам пишет
портативные копии `scripts/config.yml` + `scripts/tunnel-creds.json`.
Залей всю папку бота на VDS → запусти `start.bat` → домен живёт.
Скрипты запускать НЕ обязательно (нужны только для первой авторизации
Cloudflare — делается один раз на домашнем ПК).

Панель за доменом дополнительно закалена: сама перекидывает `http → https`
(Cloudflare проксирует оба, а «домен не защищён» убивается редиректом) и шлёт
HSTS-заголовок — браузер запоминает «только https».

(Конфиг туннеля можно держать и в `scripts/cloudflared + config.yml` —
скрипт кладёт `cloudflared.exe` туда, конфиг пишется в профиль пользователя
`%USERPROFILE%\.cloudflared\config.yml`; бот найдёт оба варианта.)

## Шаг 3. Перезапуск бота

Скрипт уже дописал в `.env` строку `WEB_BEHIND_PROXY=1` (если не нашёл `.env`,
он скажет — тогда допиши руками). Эта строка включает ProxyFix: панель
корректно понимает `https://` за туннелем, иначе ломаются редиректы и часть
форм. Просто **перезапусти `start.bat`**, чтобы она подхватилась.

## Шаг 4. Пользуемся

- **https://hakumods.xyz** — панель (логин `owner` + твой пароль).
- Также открывается: `https://www.hakumods.xyz`, `https://panel.hakumods.xyz`.
- Ссылки **никогда не меняются** — перезагрузка ПК ничего не сбивает,
  туннель поднимается сам.

## Безопасность (важно!)

- Панель теперь видна всему интернету на главном домене → пароль владельца
  должен быть крепким (сменить: Профиль → Сменить пароль, либо `PANEL_PASSWORD`
  в `.env`).
- Ссылку никому не давай: внутри — полное управление сервером.
- Бонус позже: Cloudflare Zero Trust → Access — второй замок на входе
  (код на e-mail). Скажи — распишу.

## Если что-то идёт не так

| Симптом | Причина | Лечение |
|---|---|---|
| Домен не открывается вовсе | NS ещё не переключились | Подождать; статус смотреть в Cloudflare |
| Ошибка 502 / 1033 | Туннель упал или бот выключен | Запустить бота; проверить службу `cloudflared` |
| Логин «выкидывает» после входа | Нет `WEB_BEHIND_PROXY=1` | Добавить в `.env`, перезапустить бота |
| Захотел вернуть сайт-пустышку | — | Удалить CNAME `@` в Cloudflare и вернуть `A @ → 65.108.15.136` |
