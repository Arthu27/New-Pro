# Aether (MOEBIUS) — Discord Bot

Русский Discord-бот с AI-функциями, модерацией и веб-панелью управления.

## Возможности

- **AI-чат** с поддержкой контекста (Mistral / OpenRouter / DeepSeek / локальная Ollama)
- **AI-модерация и AI-тикеты** — автоматическая категоризация, анализ тональности, RAG
- **Модерация** — бан, кик, мут, варны, временные наказания, автомод, антирейд
  - **📁 Демки к наказаниям** — `/proof` или параметр «демка» прямо в `/warn` и `/moderate`:
    доказательство перезаливается в канал `#-доказательства`, админ листает канал и видит всё
  - **😶 Тихий мут** — `/ghostmute`: сообщения нарушителя незаметно исчезают, он не в курсе
  - **🚨 Panic-локдаун** — `/panic on`: все каналы под замок одним нажатием, точный откат
  - **🕵️ /snipe** — последнее удалённое/изменённое сообщение; **📌 /stick** — липкие сообщения
  - **⚡ Варн реакцией** — мод ставит ⚠️ на сообщение → автор получает варн; **☢️ /nuke** —
    пересоздание канала; **🧹 /raidcleanup** — кик/бан набежавших; **🧽 /dehoist** — чистка ников;
    **🧾 /case** — досье нарушителя одной PNG-карточкой (варны, демки, заметки, скор)
- **🧹 Автофильтр чата** — запрещённые слова (ловит л33т, повторы букв, раздельный ввод),
  анти-ссылки с whitelist, анти-капс, анти-флуд с таймаутом; авто-варн через систему варнов,
  исключения каналов/ролей, настройка из панели `/autofilter` и командами `/filter`
- **🌐 Публичная статус-страница** — `/status` без логина: онлайн, пинг, аптайм, число серверов
- **💾 Автобэкапы данных** — zip-архив `data/` каждый день по расписанию, ротация старых,
  скачивание из веб-панели; секреты панели в архив не попадают
- **Экономика, уровни, розыгрыши, миниигры**
- **Веб-панель** — аналитика, управление сервером, тикеты, бэкапы, демки, мод-инструменты
- **Музыка, голосовые команды, TTS**
- **Утилиты** — дни рождения, AFK, реакция-роли, приветствия, starboard, опросы
- **Автоматика нового поколения:**
  - **🌙 Ночной режим** — `/ночь`: слоумод и лок каналов по расписанию (напр. 23:00–07:00),
    утром возвращает прежние права точь-в-точь; каналы-исключения, репорты в лог
  - **👶 Анти-альт** — `/антиальт`: ловит свежесозданные аккаунты при входе
    (возраст < N дней), действие на выбор — тревога/кик/бан, белый список
  - **🔒 /lockdown** — аварийное закрытие канала или всего сервера на запись со
    снимком исходных прав; `/unlockdown` возвращает именно их, а не «на глазок»;
    переживает рестарт бота
  - **✍️ Апелляции** — забаненный пишет боту в ЛС `/апелляция`, модераторы решают
    кнопками прямо в карточке (переживают рестарт — persistent views)
  - **📰 Мод-дайджест** — еженедельная сводка: сколько мутов/варнов/банов, кто из
    модераторов активнее, самый горячий день; `/дайджест [дней]` — мгновенно
  - **⏰ Напоминания** — `/напомни "через 10м" текст`, повторы «каждый день …»,
    доставка в канал или ЛС, список и отмена
  - **📊 /recap** — «что я пропустил?»: сводка канала за N часов без AI — топ
    авторов, слова периода, пик активности, самая зареагированная реплика
  - **🏆 Достижения** — ачивки за сообщения/войс/стаж с анонсами в канал,
    `/ачивки` карточка с очками, `/ачивлидеры` — топ сервера
  - **🗂 Шаблоны сервера** — `/шаблон сохранить`: снимок ролей и каналов в
    переносимый JSON; `/шаблон применить` создаёт недостающее, существующее не трогает
  - **👋 Приветствия PRO** — ротируемые шаблоны с переменными `{mention} {server}
    {count}`, карточка с аватаром, ЛС новичку, `/приветствие тест` — превью
  - Настройки всей автоматики редактируются из панели — страница **«Автоматика»**:
    панель и коги читают одну и ту же SQLite-точку, перезапуск не нужен
- **🔢 Считалка** — `/счёт канал`: сервер считает по очереди, сбой — и с 1, рекорд сервера хранится
- **❤ Карма** — `/спасибо @user` или реакция ➕/👍 на чужом сообщении; кулдаун против накруток, `/карматоп`
- **💬 Триггеры** — `/триг добавить слово | ответ`: автоответы по ключевым словам (точное слово или подстрока, кулдаун, счётчик срабатываний)
- **📈 Каналы-счётчики** — `/счётчики добавить #канал Участники: {members}`: живое табло
  в списке каналов (участники/онлайн/каналы/бусты), авто-обновление с уважением к рейт-лимитам
- **⚔ Дуэли** — `/дуэль @user`: вызов с кнопками, честная монетка 50/50, серии побед и топ
- **🎯 Квиз-машина** — `/квиз старт`: викторины в чате, очко за первый верный ответ,
  своя библиотека вопросов (`/квиз добавить`) и сезонный топ (`/квиз топ`)
- **🕐 Стафф-смены** — `/дежурства назначить @user пт 18:00-22:00`: недельное расписание
  дежурств, автопинг при старте смены, благодарность и анонс следующего в конце, смены через полночь
- **📄 HTML-экспорт логов** — `/логи-экспорт 14` или кнопка в панели: журнал модерации
  автономным HTML-файлом (фильтры по дням/категории/модератору, печать, XSS-safe)
- **⭐ Рейтинг стаффа** — `/оценить @мод 1-5`: народный балл модераторов, зачётка с
  комментариями, один голос на человека (переголосование заменяет)
- **Тесты** — `tests/` 111 наборов, 5073 проверок, прогон без токена Discord
- **Бюджет слеш-команд** — в глобальном меню Discord максимум 100 команд;
  `slash_budget.py` держит в меню кураторские ~45 (модерация + ежедневное),
  остальные ~147 команд живут через префикс — ничего не теряется, меню не переполняется
  (`scripts/run_tests.py`)

## Установка

```bash
pip install -r requirements.txt
cp .env.example .env   # затем заполните TOKEN и ключи
python main.py
```

Бот при старте сам проверяет и доустанавливает недостающие зависимости.

### Обновление (Windows)

`update.bat` в корне — двойной клик: скачивает свежую сборку ветки,
докладывает код поверх (`.env`, `data\`, логи и окружение не трогаются),
при необходимости ставит зависимости и перезапускает бота.
Режим без рестарта: `update.bat /norestart`.

## Запуск через Docker

```bash
docker compose up -d
docker compose logs -f
```

## Веб-панель

Адрес: http://localhost:5001

**Дизайн — «Light Edition Premium»**: светлый минимализм с премиум-слоем —
стеклянная шапка, градиентные акценты, свечения, каскадные анимации, тёмная
тема по переключателю. Единая дизайн-система в `web/static/style.css`,
клиентский кит `web/static/app.js` (тосты, подтверждения, палитра Ctrl+K,
live-refresh, SVG-графики без внешних библиотек, сортировка таблиц,
анимированные счётчики). Главная — «Командный центр»: живые графики
активности модерации, типы действий, быстрые переходы, угрозы и здоровье
бота. Раздел «Модерация» перестроен с нуля: 18 инструментов в четырёх
подразделах — Реагирование, Расследование, Защита, Команда и аналитика.
Палитра Ctrl+K (или Alt+M) ищет страницы, сущности сервера (участники,
транскрипты, триггеры, объявления) и выполняет действия: смена темы,
копирование ссылки, выгрузка CSV, переходы. На главной — живой «Пульс
панели» (лента событий в реальном времени), настройка видимых виджетов,
спарклайн пинга за сессию, мини-расписание смен; графики с интерактивными
тултипами, 3D-tilt карточек, выбор акцента интерфейса, aurora-фон.
Публичные страницы (/status, гостевая главная) — в том же стиле.
Новый «Оперативный центр» (/ops-center) — живое состояние бота, CPU/RAM
с историей сессии, журнал действий панели, хвост живых логов бота и
операционные переходы. «Серверы» и «Пользователи» пересобраны на новых
компонентах (премиум-карточки, статусы присутствия, карточка участника).
Крупные модули: «Доска команды» (/team-board) — канбан модерации с
drag&drop, приоритетами, ответственными и сроками, хранение на сервере;
«Студия темы» (/theme-studio) — акцент, скругления, масштаб, плотность
и тема с живым превью и сохранением за учёткой (применяются на всех
страницах); центр уведомлений v2 с табами «Все/Системные/Личные» и
отметкой прочитанного. AI-чат пересобран (пузыри, быстрые подсказки,
анимация набора, глянцевая отправка), экономика — премиум-KPI, медали
топ-3, живые счётчики. Мобильная нижняя навигация, чип серверов в
сайдбаре, пульс дашборда реагирует на WebSocket-события.
Новый «Центр модерации» (/mod-center) — сводный штаб: живые метрики
(активные меры, варны, апелляции, демки, участники на грани), графики
активности, структура мер, теплокарта, лента действий и сетка всех
19 инструментов раздела с быстрыми действиями. Инструменты усилены:
фильтры по типу действия и модератору в журнале, группировка варнов
по участникам с переходом в досье, поиск во временных мерах, период
в демках, глубокие ссылки ?user= в аналитике рисков.
FX-слой: созвездие частиц на фоне, свечение за курсором, загрузочный
сплэш, вращающиеся аура-рамки, вспышки значений, параллакс фона,
календарь активности на 90 дней (GitHub-стиль), конфетти при успехах,
свёртываемый сайдбар, фокус-режим, избранные страницы, подсветка
поиска в таблицах, тултипы, справка по горячим клавишам, тонировка
фона под каждый раздел. Качество (та же палитра): плавные анимации меню,
кастомные чекбоксы/радио/селекты, градиентные заливки и свечение
графиков, выделение строк в журнале с выборочным CSV-экспортом,
копирование ID кликом, кнопки паузы live и плотности таблиц,
штампы «обновлено», зебра-таблицы с липкими шапками. Пересобраны
OPS-центр тикетов (сортировка, массовое закрытие, карточки с заметками),
поиск участников (быстрые чипы, карточка профиля) и статистика AI
(кольцо решений, бары причин, медали топа нарушителей); календарь
активности добавлен на главную.
Стеклянный стиль: сайдбар, панели, карточки, KPI и палитра — полупрозрачное
стекло поверх aurora-фона; акцентные полоски KPI по тону, авто-иконка
раздела в заголовке каждой страницы, тень шапки при прокрутке, тонированные
иконки плиток инструментов.
Красота v3: стеклянный блик при наведении, «дышащие» иконки разделов,
аура бренда, мерцание заголовков (sheen), пружинные дроверы, анимация
появления сообщений чата, пульс бейджей, свечение активных вкладок,
градиентная кнопка «наверх», бегущие блики по линиям секций.
Красота v4: плывущий градиент primary-кнопок, переливы прогресс-баров,
анимированная галочка в тостах (SVG-штрих), тряска ошибок, эффект печати
заголовка главной, пружинное открытие палитры, покачивание иконок KPI.
FX-слой 3: плавающее меню быстрых действий (Alt+N), интерактивный тур по
панели с подсветкой, перестановка виджетов главной, звук уведомлений
(WebAudio, отключаемый), каскад KPI; статистика бота пересобрана на
собственном графическом движке (без внешних библиотек).
FX-слой 4: дрейфующая сетка в hero, авто-тема по системе при первом визите,
счётчик непрочитанных в заголовке вкладки браузера, анимация переключения
вкладок, «загрузить ещё» в журнале, живой градиент бренда, стеклянные
кнопки шапки.
FX-слой 5: плавающий индикатор пункта меню (скользит за курсором и
останавливается на активном), вырастающие бары графиков, рисующиеся
волной сегменты колец, точки графиков после линии, аврора реагирует на
мышь, частицы разбегаются от курсора, орбитальные точки у пустых
состояний, иконки палитры в градиентных чипах, дрейфующий градиент
бренд-панели логина.
FX-слой 6: перетаскивание виджетов главной (drag&drop с ручками и
индикаторами), мини-график активности в сайдбаре, «Контур спокойствия»
в центре модерации (соотношение снятий и наказаний), недавние страницы
в палитре, оффлайн-баннер, печать отчёта модерации.
FX-слой 7: кольца загрузки в кнопках, командная строка «Быстрое действие»
в центре модерации (набор текста → переход в инструмент), экспорт календаря
активности в CSV, живые тосты событий бота (WebSocket), покачивание иконок
KPI, акцентная кромка карточек доски, анимация обновлённых чисел.
FX-слой 8: живая пилюля пинга в шапке (зелёный/жёлтый/красный, клик — в
статистику бота), Enter-фокус поиска из пустых состояний с чипами-
подсказками, мигающий курсор в командной строке, улучшенные тултипы.
FX-слой 9: страница чата полностью пересобрана в светлой дизайн-системе
(градиентные аватары ботов, чип-разделители дат, светлые попапы профиля и
подтверждений, свечение фокуса поля ввода), живой градиентный обод
карточек при наведении, параллакс панелей при прокрутке, конфетти при
входе в панель, drag-сортировка избранного в сайдбаре, плотность таблиц
следует за учёткой (сохраняется на сервере).
FX-слой 10: кокпит аналитики переведён на светлую дизайн-систему с
индиго-циановыми градиентами; тонкая градиентная полоса прогресса
прокрутки вверху страницы; кнопка «наверх» с кольцом, показывающим
прогресс; магнитные кнопки (слегка тянутся к курсору); пробегающий блик
по панелям при наведении; пульс-кольцо FAB. Тёмное «золотое» наследие
в шаблонах переведено на токены палитры (золотые тинты — в индиго через
color-mix, тёмные фоны — в var(--bg), белые подложки — в серые токены).
Чистота интерфейса: искры-точки после клика, звёздный бурст избранного
и свечение, следующее за курсором (глобальное и внутри карточек), убраны
по просьбе владельца — красота без навязчивых эффектов; аврора реагирует
только на прокрутку; лента событий мод-центра стала живой: плавный
авто-дрейф ленты с паузой при наведении.
Единые премиум-шапки: страницы без page-head автоматически получают
иконку-плитку, eyebrow и описание из меню; старые компактные hero
переведены на премиум-вид; дубли заголовков в навбаре скрыты. Фирменная
эмблема Aether (сгенерирована в палитре индиго) в шапке, на логине и в
качестве favicon. Плавные переходы между страницами через View
Transitions API (фейд с лёгким подъёмом, отключается при reduced-motion).
Экран дежурного (/mod-kiosk) — полноэкранная живая стена модерации:
крупные KPI, кольцо мер, контур спокойствия, топ нарушителей, нагрузка
команды, дрейфующая лента событий и авто-ротация слайдов.
FX-слой 11 «премиум-формы»: селекты с фирменным градиентным шевроном
(стрелка оживает при наведении), стилизованные опции и multi-select,
чекбоксы с pop-анимацией и indeterminate-состоянием, радио с градиентной
точкой и свечением, кастомные ползунки с белым бегунком, аккуратные
индикаторы календаря у date/time, деликатные спиннеры чисел, градиентные
переключатели с подсветкой и полировка цветового выбора.
AetherSelect: нативные «классические» списки браузера заменены полностью
кастомными дропдаунами — стильная панель в дизайн-системе с поиском (от
8 опций), галочкой выбранного, группами optgroup, клавиатурной навигацией
(стрелки/Enter/Esc), крупным вариантом для выбора сервера и встроенным
для кокпита аналитики; исходные select остаются в DOM и продолжают
рассылать change-события — вся логика страниц работает как раньше.
Стабильность прокрутки: клики по каналам и живые перерисовки больше не
сбрасывают страницу вверх — keepScrollAsync возвращает позицию после
обновления DOM, сайдбар-меню и страницы помнят прокрутку между
переходами (sessionStorage), активный пункт меню всегда виден, фокусы
инпутов работают без прокрутки (preventScroll), сворачивание категорий
каналов заякорено.
Центр модерации: живая лента событий (тикер последних мер), панели
«ТОП нарушителей за 7 дней» и «Нагрузка команды» с ранговыми барами.

Логин владельца: `owner`.

Пароль задаётся через `PANEL_PASSWORD` в `.env`. Если он не задан, при первом
запуске генерируется **случайный надёжный пароль** — он выводится в консоль
и сохраняется в `data/panel_credentials.txt` (доступ только у владельца).
После входа попадаешь сразу в панель — без обязательных ритуалов. Пароль
меняется в любой момент по желанию: **Профиль → Сменить пароль**
(txt-подсказка после смены удаляется). Пароль, сменённый через панель,
хранится в `data/panel_credentials.json` — только солёный хэш **scrypt**
(старые SHA-256/plaintext апгрейдятся сами при первом успешном логине).

## Конфигурация

Все настройки — в `.env` (шаблон: `.env.example`):

- `TOKEN` — токен бота из Discord Developer Portal
- `OWNER_ID`, `MAIN_GUILD_ID` — ID владельца и основного сервера
- `MISTRAL_API_KEY` — ключ AI (или альтернативы / локальная Ollama)
- `PORT`, `SECRET_KEY` — веб-панель
- `DISABLE_TUNNEL=1` — отключить Cloudflare Tunnel (в Docker уже задано)

### Режим модулей

- `MOD_ONLY=1` — «только модерация»: грузятся модераторские + системные модули
  (наказания, варны, кейсы, демки, автомод, анти-рейд, верификация, тикеты, журналы).
  Экономика, игры, музыка, AI-чат, ивенты и т.п. выключаются; их данные на диске
  остаются — возврат модулей поднимает всё на место.
- `DISABLED_COGS=music_cog,giveaway` — точечно выключить модули в любом режиме.
- `EXTRA_COGS=economy_cog,level_cog` — вернуть отдельные модули поверх `MOD_ONLY`.
- Логика — в `cogs_policy.py`; применяется при старте. Рантайм без рестарта:
  `!module list` / `!module load <имя>` / `!module unload <имя>` (от владельца).

## Структура

- `main.py` — входная точка бота
- `db.py` — SQLite-слой (`GuildData`/`UserData`), `json_store.py` — JSON-хранилища с кешем чтения
- `cogs_policy.py` — режимы модулей (MOD_ONLY / DISABLED_COGS / EXTRA_COGS)
- `slash_budget.py` — белый список слеш-меню (лимит Discord — 100 команд); что вернуть в меню — добавь имя в `KEEP_SLASH`
- [`cogs/`](cogs/README.md) — 107 модулей команд бота ([каталог](docs/MODULES.md))
- [`services/`](services/README.md) — сервисный слой (форматирование RU-текста, меню панели, AI, бэкапы, SLA, отчёты, плагины)
- [`web/`](web/README.md) — веб-панель (Flask): `app.py`, доменные роуты в `web/routes/`, 93 шаблона
- [`tests/`](tests/README.md) — регрессионный полигон (см. [docs/TESTING.md](docs/TESTING.md))
- [`docs/`](docs/README.md) — документация: каталог модулей, тестирование, архив заметок
- [`scripts/`](scripts/README.md) — регрессия, демо-панель, генератор каталога
- [`config/`](config/README.md) — примеры конфигов
- [`assets/`](assets/README.md) — шрифты, иконки, фоны для карточек
- `data/`, `logs/`, `backups/`, `plugins/` — создаются при запуске (в git не хранятся)

## Полезные команды

- `start.sh` / `start.bat` — запуск на Linux/Windows
- `auto_update.py` — обновление бота
- `scripts/demo_panel.py` — демо-превью панели без Discord (`:8090/demo-login`, поддельный сервер + засеянные логи/демки)
- `scripts/run_tests.py` — вся регрессия одной командой (CI-ready код выхода)
- `scripts/gen_module_docs.py --write` — пересобрать каталог модулей
- `python -m py_compile main.py` — быстрая проверка синтаксиса
Исправление AetherSelect: replaceChild вызывался после переноса select
внутрь обёртки (замена узла на предка → HierarchyRequestError), из-за
чего дропдауны не появлялись, а исключения при перерисовках создавали
лаги; теперь обёртка встаёт на место select до переноса, сбои одного
селекта изолированы (tryEnhance), а регрессия закрыта исполняемым
Node-харнессом tests/test_aether_select.py.
Производительность: вечные фоновые анимации убраны — градиентные кольца
карточек и бегущая линия панелей оживают только при наведении;
backdrop-blur снижен (сайдбар 20→8px, панели 14→6px и т.д.) — стекло
осталось, GPU-нагрузка упала; магнитные кнопки троттлятся через rAF с
кэшем rect (нет форсированного layout на каждый mousemove); частицы фона
рисуются на 30 fps и замирают в фоновой вкладке; параллакс карточек
ограничен 12 слоями.
FPS-революция: фоновый canvas частиц полностью удалён, а вместе с ним и
все backdrop-filter-блюры по панели — постоянная перерисовка канваса
заставляла каждую стеклянную поверхность переразмывать фон на каждом
кадре (эффект «FPS 10»). Стеклянный вид сохранён полупрозрачными фонами,
нагрузка на композитор упала в разы.
FPS 60+: вычищены последние постоянные перерисовки — гигантские блюры
авроры (90px) и сетки hero (70px) с их вечными анимациями удалены,
вечные блики/свечения/сдвиги градиентов кнопок, шиммеры прогресс-баров
и линий секций переведены в hover-режим или убраны, стекло стало
непрозрачным (вид тот же, перерисовок при скролле ноль), мёртвые
mix-blend-слои спотлайтов удалены, sticky-шапки вынесены в отдельные
слои, параллакс авроры на скролле отключён. На странице не осталось ни
одной анимации, работающей без взаимодействия.
120 FPS: View Transitions выключены (снапшот всего DOM на навигации давал
рывки), кольца карточек при ховере стали статичным градиентом без
вращения, пульс бренда убран, transition:all заменён точечными
свойствами, кольцо прогресса кнопки «наверх» перерисовывается не чаще
8 раз в секунду. Интерактивные эффекты сохранены — цена за кадр при
скролле и навигации сведена к композитингу.
Стабильность и плавность: блокирующий @import шрифтов убран — шрифты
грузятся асинхронно (медиа-трюк) и не задерживают рендер ни на
миллисекунду; загрузочный сплэш показывается один раз за сессию вместо
вспышки на каждой навигации; live-обновления журнала и графиков
мод-центра получили fingerprint-гарды — таблицы и графики больше не
мерцают, когда данные не изменились.
Порядок в каналах: демо-структура сервера засеяна (4 категории, 16 каналов
с позициями, темами и типами), API каналов отдаёт её в DEMO_MODE при
офлайн-боте; страница управления каналами сортирует категории и каналы по
позиции, чат-сайдбар группирует текстовые каналы по категориям
(внекатегорийные — в конце); исправлен баг falsy-нуля в серверной
сортировке.
Скрытие каналов владельцем: на странице каналов у каждой строки и у
заголовка категории появилась кнопка «глаз» — owner может скрыть канал
или целую категорию из панели (хранится в data/hidden_channels.json,
API /channels-visibility только для owner). Скрытое исчезает из списка
каналов и из чата; тумблер «Скрытые» показывает их приглушёнными с
возможностью вернуть одним кликом.
Настройки виджетов главной починены: попап позиционируется по кнопке
(fixed) и не зависит от слоёв героя, привязка кнопки выполняется первой
и не ломается, даже если какой-то загрузчик данных упадёт (все загрузчики
обёрнуты в try/catch).
Логотип: новый фирменный знак — красивый белый дракон на прозрачном
фоне (эмблема в шапке, на логине, в киоске и как favicon).
Виджеты главной: настройки переехали в полноценное модальное окно (поверх
всего, закрывается по Esc/фону); заодно исправлена глобальная беда —
.modal-overlay скрыт по умолчанию и показывается только классом .open.
@-поиск: нажми @ в любом месте панели — мгновенный поиск по страницам,
участникам, каналам, расшифровкам, триггерам и анонсам (клавиатура,
Enter — открыть).
Welcome-страница пересобрана для участников: дракон в герое, «всё для
участников» (профиль, карточка 360°, заявки, обращения, уровни, события),
3 шага старта, подсказка про @, быстрый поиск и блок для команды.
Качество 1080p: убран субпиксельный параллакс панелей (дробные
translate3d мылили текст — «эффект 144p»), сняты слои translateZ и
will-change, размывавшие текст шапки/сайдбара/карточек; добавлен полный
набор сглаживания шрифтов (webkit + moz + font-synthesis) и чёткий рендер
изображений (image-rendering + backface-visibility), магнитные кнопки
двигаются целыми пикселями, вложения в чате крупнее (480px).
Страницы заявок и каналов пересобраны в единый премиум-вид: «Заявка в
администрацию» получила шапку страницы, индикатор шагов (сервер →
Discord → анкета → отправка), красивые карточки серверов, карточку
верификации участника и экран успеха с анимацией; «Мои заявки» — статусная
лента с цветными бейджами и причинами решений; рассмотрение заявок —
светлая модалка анкеты на токенах палитры; публичная страница заявки
подключена к дизайн-системе с драконом. Все эмодзи в этих страницах
заменены иконками Font Awesome.
Заявки в команду: перенесены из «Тикетов» в Модерацию → управление и открыты
для мод+; форма получила выбор должности (Хелпер / Модератор / Чат-контроль)
с карточками-радио; API сохраняет роль и принимает оба варианта ключей
(почему/why, активен/activity).
@-пикер чата в стиле Discord: шапка «Участники», аватары со статусной
точкой, подсветка совпадения, выбор мышью при наведении, клавиатура.
Welcome-hero: градиентное кольцо вокруг дракона, дышащее гало и чипы
разделов.
Чистка каналов: все 85 «грязных» шаблонов приведены в порядок — тёмные
карточки, чужеродные цвета (#2ecc71, #e74c3c, золото) и все эмодзи
заменены на палитру и Font Awesome; тест теперь гарантирует чистоту
каждого шаблона.
Финальная полировка (пройдены все 120 страниц несколькими способами):
живой рендер каждой страницы через HTTP, рендер тест-клиентом, синтаксис
каждого JS-блока через Node, сканеры эмодзи/золота/светлых текстов.
Исправлено: золотая тема консоли и расписания заменена палитрой (белые
надписи на светлом исчезли), нечитаемые светло-зелёные/розовые тексты
заменены тёмными тонами, 12 тёмных инпутов с белым текстом в настройках
тикетов переведены на светлые, синтаксис-ошибка JS в automation.html
исправлена, демо-сессия подставляет selected_guild (все страницы с
выбором сервера открываются). В шаблонах: ноль эмодзи, ноль старого
золота, ноль белых писем.
Все страницы панели пересобраны в единый премиум-вид: 60 страниц со старым
компактным hero переведены на полноценный page-head (eyebrow с группой
меню, крупный заголовок, описание), 39 страниц без шапки получили
настоящие page-head, 7 страниц с живыми динамическими hero (статусы AI/
здоровья, приветствие) сохранили свои уникальные шапки. Каждая из 120
страниц меню проверена живым обходом и рендером тест-клиентом: 200,
шапка на месте, заголовок один, битой разметки нет.
Правила сервера пересобраны с нуля: премиум-панели редактора и
предпросмотра, пронумерованные строки правил с кнопками вверх/вниз и
удалением, превью в стиле Discord-сообщения, выбор канала публикации,
тосты вместо alert(), русские тексты вместо турецких, API публикации
отправляет эмбед в индиго-цвете с русским футером, демо-правила засеяны.
Канальные страницы — финальная полировка: в транскриптах убраны золотые
акценты и тёмная модалка (#171410 → светлая), в объявлениях — остатки
золотых рамок; в дизайн-систему добавлены недостающие стили: .stat-row
(сетка статистики), .btn-new (кнопка «Новый канал»), .profs-role
(контейнер ролей профиля), .ann-meta (пустые подписи), тонированные
иконки статистики gold/cyan/purple. Все 6 канальных страниц отдают 200,
JS-блоки синтаксически чисты, эмодзи и тёмных фонов нет.
Справка пересобрана с нуля: живые карточки всех 120 инструментов из
реального меню панели (кликабельные, с иконками, описаниями и цветовыми
тонами групп), поиск по инструментам с фильтрами-чипами по группам меню
и счётчиками, настоящие горячие клавиши панели (@, Ctrl+K, Alt+M, Alt+N,
Shift+F, Esc) вместо бесполезных браузерных (F12/инкогнито убраны),
FAQ-аккордеон со ссылками на разделы, кнопка открытия окна подсказки.
Левелинг пересобран: страница «Уровни» получила премиум-панели настроек
(переключатели со свечением, поля XP, выбор канала), таблицу лидеров с
медалями за топ-3 и анимированными XP-барами, тосты вместо капс-статусов,
живую загрузку серверов из API (мёртвый копипаст-фолбэк убран); в
leveling-admin исправлен несуществующий токен --bg-3 (карточки были без
фона) и переведены на палитру медали, редкости достижений и бегунки
переключателей; демо-опыт и настройки засеяны.
Большая полировка страниц: чат полностью работает в демо (сообщения,
отправка, удаление, участники для @-пикера и DM — без бота, хранилище
data/chat_demo.json); единый блок стилей для бэкапов, дежурств, команд
(карточки + модалка), ролей, доступа, настроек и поиска участников;
кнопки сохранения стали градиентными премиум-кнопками вместо белого
стандарта; дубль «Поиск» убран из меню — поиск живёт в карточке 360;
устранён полночный флейк теста дежурств.
Welcome-страница переведена в премиальный чёрный: глубокая палитра
(#05060a, стеклянные поверхности), цветные свечения и сетка на фоне,
вращающееся градиентное кольцо вокруг дракона с дышащим гало, заголовок
«Управляй сервером красиво», тёмные стеклянные карточки секций,
сохранены чипы, подсказка про @ и быстрый поиск.
Welcome стал ещё живее: звёздное небо на canvas (пауза в фоновой
вкладке), мягкий параллакс цветных свечений за мышью, секции проявляются
при прокрутке, заголовок печатается с мигающим курсором, живые счётчики
(серверы/участники/пинг из /api/stats с фолбэком), бегущая лента фич с
паузой при наведении, блеск primary-кнопок и градиентные кольца карточек
при hover. /welcome внесён в whitelist публичных эндпоинтов.
Welcome-полировка: @ стал автоматическим — поле поиска само ждёт ввода
(автофокус на десктопе, без лишних кликов), поиск бьёт в живую панель
(/api/ux/search: страницы, участники, каналы, транскрипты, триггеры,
анонсы) с мгновенным локальным фолбэком; подсказки переформулированы —
«Просто печатай», а не обязательный шаг; заголовок печатается с ротацией
четырёх фраз.
Welcome — премиум-апгрейд: живой терминал-демо «Панель в деле»
(команды набираются с курсором, выполняются сценарии: варн, локдаун,
@-поиск, анонс — по циклу), созвездие из звёзд с соединяющими линиями,
scroll-индикатор с бегущим колесом мыши, вращающаяся градиентная рамка
кнопки входа, градиентные акценты в заголовках секций.
Подсказки ников починены везде: общий источник демо-участников в
_common (DEMO_MEMBERS + demo_members_search), страница поиска участников
отдаёт результаты при офлайн-боте, @-поиск панели включает участников,
подсказки карточки 360 дополнены именами демо-участников. Ввод ника
теперь предлагает людей в чате, поиске и карточке.
